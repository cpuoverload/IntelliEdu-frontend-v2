export enum AppType {
  Grade = 0,
  Evaluation = 1,
}

export enum AuditStatus {
  Pending = 0,
  Approved = 1,
  Rejected = 2,
}

export enum ScoringStrategy {
  Custom = 0,
  AI = 1,
}

export enum UserRole {
  User = "user",
  Admin = "admin",
  VIP = "vip",
}
